TODO: create a custom npm package on the periodic table

TODO: add these tasks to package.json after that
        "test": "jest",
        "watch-test": "npm run test -- --watchAll",
        "test:watch": "jest -o --watch",